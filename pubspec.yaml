name: flodo_task_manager
description: Task Manager App (Flodo Assignment)
publish_to: 'none'

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  path_provider: ^2.0.15
  uuid: ^4.0.0
  provider: ^6.0.5
  shared_preferences: ^2.2.2

flutter:
  uses-material-design: true
